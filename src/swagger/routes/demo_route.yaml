
/api/demo:
  get:
    tags:
      - Recipes
    summary: this is a demo
    requestBody:
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Recipe"
    responses:
      '200':
        description: Recipe created successfully

  post:
    tags:
      - Recipes
    summary: this is a demo
    requestBody:
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Recipe"
    responses:
      '200':
        description: Recipe created successfully




components:
  schemas:
    Demo:
      type: object
      properties:
          name:
            type: string
