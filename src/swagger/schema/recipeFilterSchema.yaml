components:
  schemas:
    RecipeFilter:
      type: object
      properties:
        and:
          type: object
          properties:
            mainIngredient:
              type: string
            isVeg:
              type: boolean
            
